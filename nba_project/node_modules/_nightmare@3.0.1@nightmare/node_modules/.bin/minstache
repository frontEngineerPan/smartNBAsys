#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_minstache@1.2.0@minstache/bin/minstache" "$@"
  ret=$?
else 
  node  "$basedir/../../../_minstache@1.2.0@minstache/bin/minstache" "$@"
  ret=$?
fi
exit $ret
